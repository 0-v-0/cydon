template[shadowroot=open]
	style{
		button
			@apply px-3 text-lg
		@unocss-placeholder
	}
	.flex.items-center
		{$value}
		slot[n=increase]
			btn[@click=inc]{+1}
		slot[n=decrease]
			btn[@click=dec]{-1}
